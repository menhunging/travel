<?php if (!defined("B_PROLOG_INCLUDED") || B_PROLOG_INCLUDED !== true) die();
/*
use App\Helper;

$arResult['COUNTRY'] = [];
$arResult['REGION'] = [];

if ($countryValues = Helper\Iblock::getPropertyEnums($arParams['IBLOCK_CODE'], 'COUNTRY')) {
    $arResult['COUNTRY'] = array_combine(array_column($countryValues, 'ID'), array_column($countryValues, 'VALUE'));
}

if ($regionValues = Helper\Iblock::getPropertyEnums($arParams['IBLOCK_CODE'], 'REGION')) {
    $arResult['REGION'] = array_combine(array_column($regionValues, 'ID'), array_column($regionValues, 'VALUE'));
}
*/